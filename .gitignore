# Claude configuration files
.claude/settings.local.json

# Claude: Local debug files
claude/debug/

# Claude: (Old-) Local files
claude-old/

# Claude Code: Runtime-generated directories (not relevant for other users)
claude/file-history/
claude/history.jsonl
claude/ide/
claude/projects/
claude/shell-snapshots/
claude/statsig/
claude/todos/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor files
*.swp
*.swo
*~

# Local configuration files (user-specific, not tracked)
*.local
# Note: *.local.example files ARE tracked as templates
!*.local.example

# Environment variables (contains sensitive data like API keys)
.env
.env.local
.env.*.local

# Installation backups
*.backup
install.sh.backup

# Old claude directory (deprecated - use agents/claude instead)
# Note: Will be removed after migration is complete
/claude/
